const Header: React.FC = () => {
  return (
    <div className="flex items-center justify-center bg-notFoundBackground p-4 absolute w-full">
      <p className="text-4xl uppercase font-semibold text-notFoundText">
        Rate It
      </p>
    </div>
  );
};

export default Header;
